<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.okdeer.mall.activity.mapper.HomeIconAreaMapper" >
    <resultMap id="BaseResultMap" type="com.okdeer.mall.activity.entity.HomeIconArea" >
        <id column="id" property="id" jdbcType="VARCHAR" />
        <result column="icon_id" property="iconId" jdbcType="VARCHAR" />
        <result column="city_id" property="cityId" jdbcType="VARCHAR" />
    </resultMap>

    <sql id="Base_Column_List" >
        id, icon_id, city_id
    </sql>

    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.String" >
        select 
        <include refid="Base_Column_List" />
        from activity_home_icon_area
        where id = #{id,jdbcType=VARCHAR}
    </select>
    
    <select id="findListByHomeIcon" resultMap="BaseResultMap" parameterType="java.lang.String" >
        select 
        <include refid="Base_Column_List" />
        from activity_home_icon_area
        where icon_id = #{iconId}
    </select>

    <insert id="add" parameterType="com.okdeer.mall.activity.entity.HomeIconArea" >
        insert into activity_home_icon_area
        <trim prefix="(" suffix=")" suffixOverrides="," >
            <if test="id != null" >
                id,
            </if>
            <if test="iconId != null" >
                icon_id,
            </if>
            <if test="cityId != null" >
                city_id,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides="," >
            <if test="id != null" >
                #{id,jdbcType=VARCHAR},
            </if>
            <if test="iconId != null" >
                #{iconId,jdbcType=VARCHAR},
            </if>
            <if test="cityId != null" >
                #{cityId,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>
    
    <insert id="insertMore" parameterType="java.util.List">
		INSERT INTO activity_home_icon_area (
			id, icon_id, city_id
        ) VALUES
		<foreach collection="list" index="index" item="item" separator=",">
			(
				#{item.id}, #{item.iconId}, #{item.cityId}
			)
		</foreach>
	</insert>

    <update id="update" parameterType="com.okdeer.mall.activity.entity.HomeIconArea" >
        update activity_home_icon_area
        <set >
            <if test="iconId != null" >
                icon_id = #{iconId,jdbcType=VARCHAR},
            </if>
            <if test="cityId != null" >
                city_id = #{cityId,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=VARCHAR}
    </update>

    <delete id="delete" parameterType="java.lang.String" >
        delete from activity_home_icon_area
        where id = #{id,jdbcType=VARCHAR}
    </delete>
    
    <delete id="deleteByHomeIconId" parameterType="java.lang.String" >
        delete from activity_home_icon_area
        where  icon_id = #{iconId}
    </delete>

</mapper>